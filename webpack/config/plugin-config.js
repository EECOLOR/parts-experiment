// TODO: help compile plugins with the correct format
//
// This should be a webpack config that is specialized for plugins, we need this config for ourselves
// so why not expose it to the public and make it easier for them to compile their plugin?
